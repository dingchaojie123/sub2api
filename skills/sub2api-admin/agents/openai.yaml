interface:
  display_name: "Corgi Admin"
  short_description: "Manage Corgi admin accounts and settings"
  default_prompt: "Use $sub2api-admin to inspect and manage Corgi backend accounts safely."

policy:
  allow_implicit_invocation: true
